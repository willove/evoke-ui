node_modules/
dist/
*.log
.DS_Store
.env
.env.local
*.local

# 服务器部署配置（含真实 IP，不进 git）
scripts/.env.deploy

# VitePress：只忽略构建产物与缓存，config / theme / 插件源码必须进 git
**/.vitepress/cache/
**/.vitepress/dist/

# AI 工具会话状态
.mimosa/

# Playwright：测试产物不入 git（视觉基线 visual/**-snapshots/ 必须进 git）
test-results/
playwright-report/

# 覆盖率报告（CI 存档产物，本地 pnpm test:coverage 重新生成）
coverage/

# 内部开发文档（路线图 / 迁移计划等，不随仓库发布）
.dev-docs/
.zcode/

.agents/
plans/

# pnpm 本地 store（自建 store-dir 时不要入库）
.pnpm-store/
