# @wil-works/ebui-example-ai

AI 运营助手工作台示例：EbAiConsole + AppLayout 后台骨架，演示 transport 注入、深度思考流、
联网检索引用、场景路由与停止生成（mock 实现，无真实网络请求）。

```bash
pnpm install
pnpm --filter @wil-works/ebui-example-ai dev   # http://localhost:8629
```
