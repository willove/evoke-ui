# AGENTS.md

## UI 与文案任务必走规范

- 任何"写页面/组件布局/工具面板/落地页"的任务：开工前先读 `.agents/skills/frontend-ui-ux/SKILL.md`（定调三问 → 功能披露 → 截图自验证）。
- 任何"写宣传文案/卖点/界面说明文字"的任务：开工前先读 `.agents/skills/minimal-copy-disclosure/SKILL.md`（卖点提炼 → 说明五档下沉 → 删字测试）。
- 两套规范共同的硬要求：交付前用浏览器截图逐条自查，没有截图不许报"完成/变好了"。

## evoke-ui 整页组装硬规则

用 evoke-ui 拼整页（官网/落地页/案例页）时，除上述规范外还守：

- **EvSection 默认定宽居中**（`width` 档：narrow 920 / default 1152 / wide 1360）；通栏 `width="full"` 仅限出血大件——跑马灯、整宽图表、分屏滚动场景、出血大图，且要能说出哪个子元素是出血大件。
- **标题与主体共用同一个容器**：区块的左右边界对整个区块生效一次。禁止只给主体内层 `div` 手写 `max-width`——那会让标题通栏、卡片居中，同一区块出现两条左边界。
- **宽度只来自档位令牌**：页面私写的像素 `max-width` 一律禁止；比档位更窄的阅读测宽（如描述 560px）下沉到组件内部排版，不用第二个容器实现。
- **判定（截图可查）**：每个标准区块，标题左边缘与主体左边缘 x 相同（居中对齐的区块量中轴）；全页只有「内容容器边」与「出血大件边」两种边界，找不到第三种像素值。
